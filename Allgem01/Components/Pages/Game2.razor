@page "/parkour/play"
@rendermode InteractiveServer
@inject NavigationManager NavigationManager
@inject IJSRuntime JSRuntime
@using Allgem01.Logic
@inject Repository repository

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Allgem</title>
</head>
</html>


<header><strong>ALLGEM</strong>
    <li><button class="home" type=".button" @onclick="homeFf">Home</button></li>
    <li><button class="aboutus" type=".button" @onclick="aboutusFf">About Us</button></li>
    <li><button class="contactus" type=".button" @onclick="contactusFf">Contact Us</button></li>
</header>

<section class="downheader"></section>

<section class="lowersection">
    <div class="cards">
        @if (repository.hasPremium)
        {
            <button class="qpbutton"><img src="/quickplay.png" class="qpimage">
            <p class="qptxt">QUICK PLAY</p></button>
            <button class="csbutton"><img src="/crserver.png" class="crservericon">
            <p class="crtxt">CREATE CUSTOM SERVER</p></button>
        }
        else
        {
            <button class="qppbutton"><img src="/quickplay.png" class="qpimage">
            <p class="qptxt">QUICK PLAY</p></button>
            <button class="crbutton"><img src="/crserver.png" class="crservericon">
<div class="container">
    <div class="card_box">
        <span></span>
    </div>
</div>
            <p class="crtxt">CREATE CUSTOM SERVER</p></button>
        }
    </div>
</section>


<style>
    header {
        background: rgb(10, 13, 20);
        background-size: 300% 300%;
        color: white;
        font-size: 16px;
        padding: 20px 40px 10px 50px;
        border: none;
        border-radius: none;
        
    }
    .downheader {
        background: rgb(31, 33, 48);
        height: 2px;
        width: 100%;
        display: flex;
        justify-content: center;
    }
    li{
        display: inline-block;
        list-style-type: none;
        padding-right: 40px;

    }
    .home{
        background: none;
        background-size: 300% 300%;
        color: white;
        font-size: 16px;
        padding: none;
        border: none;
        border-radius: 8px;
        width: 70px;
        height: 35px;
        margin-left: 900px;
    }
    .aboutus{
        background: none;
        background-size: 300% 300%;
        color: white;
        font-size: 16px;
        padding: none;
        border: none;
        border-radius: 8px;
        width: 90px;
        height: 35px;
    }
    .contactus{
        background: none;
        background-size: 300% 300%;
        color: white;
        font-size: 16px;
        padding: none;
        border: none;
        border-radius: 8px;
        width: 103px;
        height: 35px;
    }
    .lowersection {
        display: flex;
        justify-content: center;
        height: 95vh;
        width: 100%;
        background: linear-gradient(to right,rgb(12, 18, 56),rgb(17, 18, 34),rgb(18, 19, 32),rgb(15, 15, 24),rgb(18, 19, 32),rgb(18, 19, 32),rgb(11, 18, 71));
    }
.cards {
  display: flex;
  flex-direction: row;
  gap: 40px;
  top: 50%;
  left: 50%;
}
.qpbutton,.csbutton{
    margin-top: 300px;
    margin-left: 20px;
    background: gray;
    border-radius: 20px;
    border: 0;
    height: 300px;
    width: 270px;
    transition: 0.5s all;
}
.qpbutton:hover,.csbutton:hover{
    transform: scale(1.1);
}
.qptxt{
    color: white;
    font-weight: bold;
    font-size: 15px;
}
.crtxt{
    color: white;
    font-weight: bold;
    font-size: 15px;
}
.crservericon{
    transform: scale(0.9);
}
.crbutton{
    margin-top: 300px;
    background: rgb(65, 65, 65);
    border-radius: 20px;
    border: 0;
    height: 300px;
    width: 270px;
}
.qppbutton{
    margin-top: 300px;
    margin-left: 20px;
    background: gray;
    border-radius: 20px;
    border: 0;
    height: 300px;
    width: 270px;
}
/* From Uiverse.io by mrhyddenn */ 
.container {
  display: flex;
  align-items: center;
  justify-content: center;
}

.card_box {
  width: 200px;
  height: 250px;
  border-radius: 20px;
  background: linear-gradient(170deg, rgba(58, 56, 56, 0.623) 0%, rgb(31, 31, 31) 100%);
  position: relative;
  box-shadow: 0 25px 50px rgba(0,0,0,0.55);
  cursor: pointer;
  transition: all .3s;
}

.card_box:hover {
  transform: scale(0.9);
}

.card_box span {
  position: absolute;
  overflow: hidden;
  width: 150px;
  height: 150px;
  top: -10px;
  left: -10px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.card_box span::before {
  content: 'Premium';
  position: absolute;
  width: 150%;
  height: 40px;
  background-image: linear-gradient(45deg, #ff6547 0%, #ffb144  51%, #ff7053  100%);
  transform: rotate(-45deg) translateY(-20px);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  box-shadow: 0 5px 10px rgba(0,0,0,0.23);
}

.card_box span::before {
  content: '';
  position: absolute;
  width: 10px;
  bottom: 0;
  left: 0;
  height: 10px;
  z-index: -1;
  box-shadow: 140px -140px #cc3f47;
  background-image: linear-gradient(45deg, #FF512F 0%, #F09819  51%, #FF512F  100%);
}
</style>

@code {
    private string? CookieValue;
    private string? CookieValue1;

    private void Graph()
    {
       NavigationManager.NavigateTo("/graph");
    }

    private void playGameShooting()
    {
        NavigationManager.NavigateTo("/shooting/play");
    }
    private void accountFf()
    {
        NavigationManager.NavigateTo("/account");
    }
    private void playGameSecurityIDM()
    {
        NavigationManager.NavigateTo("/parkour/play");
    }
    private void loginFf()
    {
      NavigationManager.NavigateTo("/login");
    }
    private void homeFf()
    {
        NavigationManager.NavigateTo("/");
    }
    private void aboutusFf()
    {
        NavigationManager.NavigateTo("/about-us");
    }
    private void contactusFf()
    {
        NavigationManager.NavigateTo("/contact-us");
    }
    private void premiumFf()
    {
        NavigationManager.NavigateTo("/premium");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Read the cookie value
            CookieValue = await JSRuntime.InvokeAsync<string>("getCookie", "allgem_auth");
            CookieValue1 = await JSRuntime.InvokeAsync<string>("getCookie", "allgem_premium");

            // Update the authentication state based on the cookie value
            if (CookieValue == "isli")
            {
                repository.SetLoggedIn(true);
            }
            else
            {
                repository.SetLoggedIn(false);
            }

            if (CookieValue1 == "hsp")
            {
                repository.SetHasPremium(true);
            }
            else
            {
                repository.SetHasPremium(false);
            }

            StateHasChanged();
        }
    }
    private async Task Logout()
    {
        await JSRuntime.InvokeVoidAsync("deleteCookie", "allgem_auth");
        await JSRuntime.InvokeVoidAsync("deleteCookie", "allgem_premium");
        repository.SetLoggedIn(false);
        NavigationManager.NavigateTo("/");
    }
}
