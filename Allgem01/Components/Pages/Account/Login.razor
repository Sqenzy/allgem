@page "/login"
@using Allgem01.Logic
@rendermode InteractiveServer
@inject NavigationManager NavigationManager
@inject Repository repository
@inject IJSRuntime JSRuntime
@using Microsoft.AspNetCore.Components.Authorization;
@using Microsoft.AspNetCore.Authentication.OAuth.Claims;
@using Microsoft.AspNetCore.Authentication;
@using Microsoft.AspNetCore.Authentication.Cookies;
@using System.Security.Claims;

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-1918445686655465"
     crossorigin="anonymous"></script>
</head>
<body>
    
</body>
</html>

<section class="boxsection">
        <p class="tg2">Welcome</p>
        <input id="p1" placeholder="Enter your email" type="email" @bind="email">
        <input id="p2" placeholder="Enter your password" type="password" @bind="password">
        <p class="ptxt">Show password</p><input type="checkbox" class="pass" onclick="p2.type = p2.type === 'password' ? 'text' : 'password'">
        <button class="login" @onclick="logIn">
            <p class="lgtxt">Log In</p><p class="txt"> ›</p>
            </button>
        <button class="signupnav" @onclick="signupFf">Create Account</button>
</section>



<style>

    .ptxt {
        position: absolute;
        left: 50%;
        transform: translate(-50%, -50%);
        margin-top: 250px;
        margin-left: 145px;
        color: white;
        font-size: 12px;
    }

    .pass {
    appearance: none;
    -webkit-appearance: none;
    outline: none;
    background: rgb(37, 37, 37);
    border: 2px solid #fff;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    cursor: pointer;
    position: absolute;
    left: 50%;
    transform: translate(-50%, -50%);
    margin-top: 250px;
    margin-left: 210px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    transition: border-color 0.2s, box-shadow 0.2s;
}

.pass:hover {
    border-color: #4a90e2;
    box-shadow: 0 4px 12px rgba(74,144,226,0.15);
}

.pass:checked {
    background: linear-gradient(135deg, #4a90e2 60%, #357ab8 100%);
    border-color: #4a90e2;
}

.pass:checked::after {
    content: '';
    display: block;
    position: absolute;
    left: 7px;
    top: 3px;
    width: 7px;
    height: 12px;
    border: solid #fff;
    border-width: 0 3px 3px 0;
    border-radius: 1px;
    transform: rotate(45deg);
}

    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background: linear-gradient(to right,rgb(12, 18, 56),rgb(17, 18, 34),rgb(18, 19, 32),rgb(15, 15, 24),rgb(18, 19, 32),rgb(18, 19, 32),rgb(11, 18, 71));
    }
    .boxsection {
        width: 550px; /* Set width */
        height: 410px; /* Set height */
        background-color: rgb(55, 56, 82);
        position: absolute;
        top: 245px;
        border-radius: 48px;
        left: 700px;
    }
    #p1{
         border-radius: 12px;
        width: 470px;
        height: 50px;     
        position: absolute;
        left: 50%;
        transform: translate(-50%, -50%);
        margin-top: 100px;
        background-color: rgb(37, 37, 37);
        color: white;
        border: none;
    }
    #p1:hover{
        outline: 1px rgb(255, 255, 255) solid;
    }
    #p1::placeholder{
        color: rgb(216, 216, 216);
        left: 5px;
    }
    #p2{
        border-radius: 12px;
        position: absolute;
        left: 50%;
        transform: translate(-50%, -50%);
        margin-top: 200px;
        width: 470px;
        height: 50px;
        background-color: rgb(37, 37, 37);
        color: white;
        border: none;
    }
    #p2:hover{
        outline: 1px rgb(255, 255, 255) solid;
    }
    #p2::placeholder{
        color: rgb(216, 216, 216);
        left: 5px;
    }
    .login {
  background: rgb(28, 75, 185);
  border: 0;
  margin-left: 213px;
  border-radius: 18px;
  transition: 0.01s outline;
  color: white;
  height: 50px;
  width: 120px;
  display: flex;
  margin-top: 300px;
}
.login:hover{
  outline: 2px rgb(202, 203, 206) solid;
}
.txt{
  margin-left: 9px;
  margin-top: 13px;
  transition: 0.25s all;
}
.login:hover .txt{
  margin-left: 17px;
  font-size: 150%;
  margin-top: 5px;
}
.lgtxt {
  margin-left: 27px;
  margin-top: 13px;
  font-weight: bold;
}
.signupnav {
  background: none;
  box-sizing: border-box;
  border: 0;
  border-radius: 20px;
  display: flex;
  transition: 0.2s all;
  align-items: center;
  font-weight: bold;
  margin-top: 10px;
  margin-left: 198px;
  color: white;
  width: 150px;
  height: 40px;
  justify-content: center;
  font-size: 15px; 
  
}

.signupnav:hover {
   background: rgb(15, 21, 36);
   transform: translateY(-3px);
}
.tg2 {
    position: absolute;
    left: 50%;
    transform: translate(-50%, -50%);
    margin-top: 35px;
    color: white;
    font-size: 30px;
}
</style>
@code {
    private string? email;
    private string? password;

    private void signupFf()
    {
      NavigationManager.NavigateTo("/signup");
    }

    private void logIn()
    {
        if (string.IsNullOrEmpty(email))
        {
            // Handle the case where email is null or empty
            JSRuntime.InvokeVoidAsync("alert", "Please input an email.");
            return;
        }

        if (string.IsNullOrEmpty(password))
        {
            // Handle the case where password is null or empty
            JSRuntime.InvokeVoidAsync("alert", "Please input a password.");
            return;
        }

        if (repository.Login(email, password))
        {
            CreateCookie("allgem_email", email, 2592000);
            CreateCookie("allgem_auth", "isli", 2592000);
            CreateCookie("allgem_rank", repository.rank, 2592000);
            CreateCookie("acm", repository.CasinoMoney.ToString(), 2592000);
            if (repository.hasPremium)
            {
                CreateCookie("allgem_premium", "hsp", 2592000);
                NavigationManager.NavigateTo("/");
            }
            else
            {
                NavigationManager.NavigateTo("/");
            }
        }
        else
        {
            JSRuntime.InvokeVoidAsync("alert", "Invalid email or password.");
        }
    }
    private async void CreateCookie ( string name, string value, int days ) 
    { 
        var test = await JSRuntime.InvokeAsync<string>("methods.CreateCookie", name, value, days); 
    }
}